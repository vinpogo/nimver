[tools]
nim = "latest"
"github:nim-lang/langserver" = { version = "1.14.0", exe = "nimlangserver" }
"github:arnetheduck/nph" = { version = "0.7.0", exe = "nph" }

[tasks.test]
run = "nimble build && nimble install && nimble test"

[tasks.build]
run = "nimble build"
